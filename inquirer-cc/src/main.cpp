#include <iostream>
#include "input.h"
#include "list.h"
#include "multi_options.h"
#include "separator.h"
#include "loading_bar.h"

int main()
{
   // input example

   // std::string a;
   // input("Enter your name", "default", a);

   // std::cout << a << std::endl;
   



   // list example

   // int b;

   // std::vector<std::string> opt = {
   //    "Order Pizza", "Wash Dishes", "Code in C++"
   // };
   
   // list(opt, "C", b);

   // std::cout << b << std::endl;
   // std::cout << opt[b-1] << std::endl;



   // multichoice example

   // std::vector<std::string> opt = {
   //    "Reward no. 1", "Reward no. 2", "Reward no. 3"
   // };
   // std::vector<std::string> user_opt = {};

   // multi_options(opt, user_opt, 2);

   // for(const auto& e: user_opt) {
   //    std::cout << e << std::endl;
   // }
   


   // separator example

   // separator(10);   
   
   

   // loading bar example

   // loading_bar(10, 100000);
   return 0;
}
